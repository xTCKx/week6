---
- name: Start en enable docker
  ansible.builtin.systemd:
    name: docker
    state: started
    enabled: yes
  become: true

- name: Restart docker
  community.docker.docker_compose:
    project_src: "{{ docker_cinnamorole_docker_compose_dir }}"
    state: restarted
  become: true
  when: docker_compose_content is defined
